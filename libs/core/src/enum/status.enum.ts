export enum StatusEnum {
  active = 'active',
  inActive = 'inActive',
}

export enum StoryStatusEnum {
  active = 'active',
  inActive = 'inActive',
  archive = 'archive',
}

export enum PostStatusEnum {
  active = 'active',
  archive = 'archive',
}
